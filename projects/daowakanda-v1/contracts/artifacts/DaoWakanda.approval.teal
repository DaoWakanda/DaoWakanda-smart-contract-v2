#pragma version 10

// This TEAL was generated by TEALScript v0.102.0
// https://github.com/algorandfoundation/TEALScript

// This contract is compliant with and/or implements the following ARCs: [ ARC4 ]

// The following ten lines of TEAL handle initial program flow
// This pattern is used to make it easy for anyone to parse the start of the program and determine if a specific action is allowed
// Here, action refers to the OnComplete in combination with whether the app is being created or called
// Every possible action for this contract is represented in the switch statement
// If the action is not implemented in the contract, its respective branch will be "*NOT_IMPLEMENTED" which just contains "err"
txn ApplicationID
!
int 6
*
txn OnCompletion
+
switch *call_NoOp *NOT_IMPLEMENTED *NOT_IMPLEMENTED *NOT_IMPLEMENTED *NOT_IMPLEMENTED *NOT_IMPLEMENTED *create_NoOp *NOT_IMPLEMENTED *NOT_IMPLEMENTED *NOT_IMPLEMENTED *NOT_IMPLEMENTED *NOT_IMPLEMENTED

*NOT_IMPLEMENTED:
	// The requested action is not implemented in this contract. Are you using the correct OnComplete? Did you set your app ID?
	err

// createApplication(string,string,uint64)void
*abi_route_createApplication:
	// endDate: uint64
	txna ApplicationArgs 3
	btoi

	// description: string
	txna ApplicationArgs 2
	extract 2 0

	// title: string
	txna ApplicationArgs 1
	extract 2 0

	// execute createApplication(string,string,uint64)void
	callsub createApplication
	int 1
	return

// createApplication(title: string, description: string, endDate: uint64): void
//
// This creates a new Dao.
// @param title The title of the new Dao.
// @param description The description of the new DAO.
// @param endDate The timestamp of the end date.
createApplication:
	proto 3 0

	// contracts/DaoWakanda.algo.ts:25
	// this.title.value = title
	byte 0x7469746c65 // "title"
	frame_dig -1 // title: string
	dup
	len
	itob
	extract 6 2
	swap
	concat
	app_global_put

	// contracts/DaoWakanda.algo.ts:26
	// this.description.value = description
	byte 0x6465736372697074696f6e // "description"
	frame_dig -2 // description: string
	dup
	len
	itob
	extract 6 2
	swap
	concat
	app_global_put

	// contracts/DaoWakanda.algo.ts:27
	// this.endDate.value = endDate
	byte 0x656e6444617465 // "endDate"
	frame_dig -3 // endDate: uint64
	app_global_put
	retsub

// getDaoDetails()(string,string,uint64,uint64,uint64,uint64,uint64,uint64)
*abi_route_getDaoDetails:
	// The ABI return prefix
	byte 0x151f7c75

	// execute getDaoDetails()(string,string,uint64,uint64,uint64,uint64,uint64,uint64)
	callsub getDaoDetails
	concat
	log
	int 1
	return

// getDaoDetails(): [string, string, uint64, uint64, uint64, uint64, AssetID, uint64]
//
// This returns all the information of the DAO in this order:
// title, description, endDate, voteCount, yesVoteCount, noVoteCount, registeredASA
// @returns Dao Details
getDaoDetails:
	proto 0 1

	// contracts/DaoWakanda.algo.ts:36
	// return [
	//       this.title.value,
	//       this.description.value,
	//       this.endDate.value,
	//       this.voteCount.value,
	//       this.yesVoteCount.value,
	//       this.noVoteCount.value,
	//       this.registeredASA.value,
	//       globals.latestTimestamp * 1000,
	//     ];
	byte 0x // initial head
	byte 0x // initial tail
	byte 0x0034 // initial head offset
	byte 0x7469746c65 // "title"
	app_global_get
	extract 2 0
	dup
	len
	itob
	extract 6 2
	swap
	concat
	callsub *process_dynamic_tuple_element
	byte 0x6465736372697074696f6e // "description"
	app_global_get
	extract 2 0
	dup
	len
	itob
	extract 6 2
	swap
	concat
	callsub *process_dynamic_tuple_element
	byte 0x656e6444617465 // "endDate"
	app_global_get
	itob
	callsub *process_static_tuple_element
	byte 0x766f7465436f756e74 // "voteCount"
	app_global_get
	itob
	callsub *process_static_tuple_element
	byte 0x796573566f7465436f756e74 // "yesVoteCount"
	app_global_get
	itob
	callsub *process_static_tuple_element
	byte 0x6e6f566f7465436f756e74 // "noVoteCount"
	app_global_get
	itob
	callsub *process_static_tuple_element
	byte 0x72656769737465726564415341 // "registeredASA"
	app_global_get
	itob
	callsub *process_static_tuple_element
	global LatestTimestamp
	int 1000
	*
	itob
	callsub *process_static_tuple_element
	pop // pop head offset
	concat // concat head and tail
	retsub

// createDaoASA(pay)uint64
*abi_route_createDaoASA:
	// The ABI return prefix
	byte 0x151f7c75

	// minimumBalanceTransaction: pay
	txn GroupIndex
	int 1
	-
	dup
	gtxns TypeEnum
	int pay
	==

	// argument 0 (minimumBalanceTransaction) for createDaoASA must be a pay transaction
	assert

	// execute createDaoASA(pay)uint64
	callsub createDaoASA
	itob
	concat
	log
	int 1
	return

// createDaoASA(minimumBalanceTransaction: PayTxn): AssetID
//
// This method creates the associated ASA for the DAO.
// @param minimumBalanceTransaction Transaction that pays the minimum balance amount
// into the contract
// @returns Asset ID
createDaoASA:
	proto 1 1

	// Push empty bytes after the frame pointer to reserve space for local variables
	byte 0x

	// contracts/DaoWakanda.algo.ts:55
	// verifyTxn(this.txn, { sender: this.app.creator })
	// verify sender
	txn Sender
	txna Applications 0
	app_params_get AppCreator
	pop
	==

	// transaction verification failed: {"txn":"this.txn","field":"sender","expected":"this.app.creator"}
	assert

	// contracts/DaoWakanda.algo.ts:56
	// assert(this.endDate.value > globals.latestTimestamp * 1000, 'Voting has ended.')
	byte 0x656e6444617465 // "endDate"
	app_global_get
	global LatestTimestamp
	int 1000
	*
	>

	// Voting has ended.
	assert

	// contracts/DaoWakanda.algo.ts:57
	// assert(!this.registeredASA.exists)
	txna Applications 0
	byte 0x72656769737465726564415341 // "registeredASA"
	app_global_get_ex
	swap
	pop
	!
	assert

	// contracts/DaoWakanda.algo.ts:58
	// assert(
	//       minimumBalanceTransaction.sender === this.txn.sender,
	//       'The mbr sender must be the same as the creator of the DAO'
	//     )
	frame_dig -1 // minimumBalanceTransaction: PayTxn
	gtxns Sender
	txn Sender
	==

	// The mbr sender must be the same as the creator of the DAO
	assert

	// contracts/DaoWakanda.algo.ts:63
	// assert(
	//       minimumBalanceTransaction.receiver === this.app.address,
	//       'The mbr receiver must be the same as the address of the DAO'
	//     )
	frame_dig -1 // minimumBalanceTransaction: PayTxn
	gtxns Receiver
	global CurrentApplicationAddress
	==

	// The mbr receiver must be the same as the address of the DAO
	assert

	// contracts/DaoWakanda.algo.ts:68
	// assert(
	//       minimumBalanceTransaction.amount > globals.assetCreateMinBalance,
	//       'The mbr amount must be greater than the minimum balance'
	//     )
	frame_dig -1 // minimumBalanceTransaction: PayTxn
	gtxns Amount
	global AssetCreateMinBalance
	>

	// The mbr amount must be greater than the minimum balance
	assert

	// contracts/DaoWakanda.algo.ts:73
	// registeredASA = sendAssetCreation({
	//       configAssetTotal: 1_000_000,
	//       configAssetName: 'DAOWAKANDA_VOTING',
	//       configAssetFreeze: this.app.address,
	//     })
	itxn_begin
	int acfg
	itxn_field TypeEnum

	// contracts/DaoWakanda.algo.ts:74
	// configAssetTotal: 1_000_000
	int 1_000_000
	itxn_field ConfigAssetTotal

	// contracts/DaoWakanda.algo.ts:75
	// configAssetName: 'DAOWAKANDA_VOTING'
	byte 0x44414f57414b414e44415f564f54494e47 // "DAOWAKANDA_VOTING"
	itxn_field ConfigAssetName

	// contracts/DaoWakanda.algo.ts:76
	// configAssetFreeze: this.app.address
	global CurrentApplicationAddress
	itxn_field ConfigAssetFreeze

	// Fee field not set, defaulting to 0
	int 0
	itxn_field Fee

	// Submit inner transaction
	itxn_submit
	itxn CreatedAssetID
	frame_bury 0 // registeredASA: uint64

	// contracts/DaoWakanda.algo.ts:79
	// this.registeredASA.value = registeredASA
	byte 0x72656769737465726564415341 // "registeredASA"
	frame_dig 0 // registeredASA: uint64
	app_global_put

	// contracts/DaoWakanda.algo.ts:80
	// return registeredASA;
	frame_dig 0 // registeredASA: uint64

	// set the subroutine return value
	frame_bury 0
	retsub

// register(asset)void
*abi_route_register:
	// registeredASA: asset
	txna ApplicationArgs 1
	btoi
	txnas Assets

	// execute register(asset)void
	callsub register
	int 1
	return

// register(registeredASA: AssetReference): void
//
// Registers an address as a voter for the DAO.
// @param registeredASA ASA ID of DAO Asa.
register:
	proto 1 0

	// contracts/DaoWakanda.algo.ts:88
	// assert(this.registeredASA.exists)
	txna Applications 0
	byte 0x72656769737465726564415341 // "registeredASA"
	app_global_get_ex
	swap
	pop
	assert

	// contracts/DaoWakanda.algo.ts:89
	// assert(this.txn.sender.assetBalance(this.registeredASA.value.id) === 0)
	txn Sender
	byte 0x72656769737465726564415341 // "registeredASA"
	app_global_get
	asset_holding_get AssetBalance
	pop
	int 0
	==
	assert

	// contracts/DaoWakanda.algo.ts:90
	// assert(this.endDate.value > globals.latestTimestamp * 1000, 'Voting has ended.')
	byte 0x656e6444617465 // "endDate"
	app_global_get
	global LatestTimestamp
	int 1000
	*
	>

	// Voting has ended.
	assert

	// contracts/DaoWakanda.algo.ts:92
	// sendAssetTransfer({
	//       xferAsset: this.registeredASA.value,
	//       assetAmount: 1,
	//       assetReceiver: this.txn.sender,
	//     })
	itxn_begin
	int axfer
	itxn_field TypeEnum

	// contracts/DaoWakanda.algo.ts:93
	// xferAsset: this.registeredASA.value
	byte 0x72656769737465726564415341 // "registeredASA"
	app_global_get
	itxn_field XferAsset

	// contracts/DaoWakanda.algo.ts:94
	// assetAmount: 1
	int 1
	itxn_field AssetAmount

	// contracts/DaoWakanda.algo.ts:95
	// assetReceiver: this.txn.sender
	txn Sender
	itxn_field AssetReceiver

	// Fee field not set, defaulting to 0
	int 0
	itxn_field Fee

	// Submit inner transaction
	itxn_submit

	// contracts/DaoWakanda.algo.ts:98
	// sendAssetFreeze({
	//       freezeAsset: this.registeredASA.value,
	//       freezeAssetFrozen: true,
	//       freezeAssetAccount: this.txn.sender,
	//     })
	itxn_begin
	int afrz
	itxn_field TypeEnum

	// contracts/DaoWakanda.algo.ts:99
	// freezeAsset: this.registeredASA.value
	byte 0x72656769737465726564415341 // "registeredASA"
	app_global_get
	itxn_field FreezeAsset

	// contracts/DaoWakanda.algo.ts:100
	// freezeAssetFrozen: true
	int 1
	itxn_field FreezeAssetFrozen

	// contracts/DaoWakanda.algo.ts:101
	// freezeAssetAccount: this.txn.sender
	txn Sender
	itxn_field FreezeAssetAccount

	// Fee field not set, defaulting to 0
	int 0
	itxn_field Fee

	// Submit inner transaction
	itxn_submit
	retsub

// vote(asset,bool)void
*abi_route_vote:
	// inFavor: bool
	txna ApplicationArgs 2
	dup
	len
	int 1
	==

	// argument 0 (inFavor) for vote must be a bool
	assert
	int 0
	getbit

	// registeredASA: asset
	txna ApplicationArgs 1
	btoi
	txnas Assets

	// execute vote(asset,bool)void
	callsub vote
	int 1
	return

// vote(registeredASA: AssetReference, inFavor: boolean): void
//
// Casts a vote.
// @param registeredASA Asset ID for DAO ASA.
// @param inFavor Indicates whether the vote is in favor of
// or against the proposal.
vote:
	proto 2 0

	// contracts/DaoWakanda.algo.ts:112
	// assert(this.registeredASA.exists)
	txna Applications 0
	byte 0x72656769737465726564415341 // "registeredASA"
	app_global_get_ex
	swap
	pop
	assert

	// contracts/DaoWakanda.algo.ts:113
	// assert(this.endDate.value > globals.latestTimestamp * 1000, 'Voting has ended.')
	byte 0x656e6444617465 // "endDate"
	app_global_get
	global LatestTimestamp
	int 1000
	*
	>

	// Voting has ended.
	assert

	// contracts/DaoWakanda.algo.ts:116
	// assert(
	//       this.txn.sender.assetBalance(this.registeredASA.value) === 1,
	//       'User is ineligible to vote as they have either not registered or already casted a vote.'
	//     )
	txn Sender
	byte 0x72656769737465726564415341 // "registeredASA"
	app_global_get
	asset_holding_get AssetBalance
	pop
	int 1
	==

	// User is ineligible to vote as they have either not registered or already casted a vote.
	assert

	// contracts/DaoWakanda.algo.ts:122
	// sendAssetFreeze({
	//       freezeAsset: this.registeredASA.value,
	//       freezeAssetFrozen: false,
	//       freezeAssetAccount: this.txn.sender,
	//     })
	itxn_begin
	int afrz
	itxn_field TypeEnum

	// contracts/DaoWakanda.algo.ts:123
	// freezeAsset: this.registeredASA.value
	byte 0x72656769737465726564415341 // "registeredASA"
	app_global_get
	itxn_field FreezeAsset

	// contracts/DaoWakanda.algo.ts:124
	// freezeAssetFrozen: false
	int 0
	itxn_field FreezeAssetFrozen

	// contracts/DaoWakanda.algo.ts:125
	// freezeAssetAccount: this.txn.sender
	txn Sender
	itxn_field FreezeAssetAccount

	// Fee field not set, defaulting to 0
	int 0
	itxn_field Fee

	// Submit inner transaction
	itxn_submit

	// contracts/DaoWakanda.algo.ts:129
	// sendAssetTransfer({
	//       xferAsset: this.registeredASA.value,
	//       assetAmount: 1,
	//       assetReceiver: this.txn.sender,
	//     })
	itxn_begin
	int axfer
	itxn_field TypeEnum

	// contracts/DaoWakanda.algo.ts:130
	// xferAsset: this.registeredASA.value
	byte 0x72656769737465726564415341 // "registeredASA"
	app_global_get
	itxn_field XferAsset

	// contracts/DaoWakanda.algo.ts:131
	// assetAmount: 1
	int 1
	itxn_field AssetAmount

	// contracts/DaoWakanda.algo.ts:132
	// assetReceiver: this.txn.sender
	txn Sender
	itxn_field AssetReceiver

	// Fee field not set, defaulting to 0
	int 0
	itxn_field Fee

	// Submit inner transaction
	itxn_submit

	// contracts/DaoWakanda.algo.ts:136
	// sendAssetFreeze({
	//       freezeAsset: this.registeredASA.value,
	//       freezeAssetFrozen: true,
	//       freezeAssetAccount: this.txn.sender,
	//     })
	itxn_begin
	int afrz
	itxn_field TypeEnum

	// contracts/DaoWakanda.algo.ts:137
	// freezeAsset: this.registeredASA.value
	byte 0x72656769737465726564415341 // "registeredASA"
	app_global_get
	itxn_field FreezeAsset

	// contracts/DaoWakanda.algo.ts:138
	// freezeAssetFrozen: true
	int 1
	itxn_field FreezeAssetFrozen

	// contracts/DaoWakanda.algo.ts:139
	// freezeAssetAccount: this.txn.sender
	txn Sender
	itxn_field FreezeAssetAccount

	// Fee field not set, defaulting to 0
	int 0
	itxn_field Fee

	// Submit inner transaction
	itxn_submit

	// contracts/DaoWakanda.algo.ts:142
	// this.voteCount.value = this.voteCount.value + 1
	byte 0x766f7465436f756e74 // "voteCount"
	dup
	app_global_get
	int 1
	+
	app_global_put

	// *if0_condition
	// contracts/DaoWakanda.algo.ts:144
	// inFavor
	frame_dig -2 // inFavor: boolean
	bz *if0_else

	// *if0_consequent
	// contracts/DaoWakanda.algo.ts:145
	// this.yesVoteCount.value = this.yesVoteCount.value + 1
	byte 0x796573566f7465436f756e74 // "yesVoteCount"
	dup
	app_global_get
	int 1
	+
	app_global_put
	b *if0_end

*if0_else:
	// contracts/DaoWakanda.algo.ts:147
	// this.noVoteCount.value = this.noVoteCount.value + 1
	byte 0x6e6f566f7465436f756e74 // "noVoteCount"
	dup
	app_global_get
	int 1
	+
	app_global_put

*if0_end:
	retsub

// manuallyCloseVoting()void
*abi_route_manuallyCloseVoting:
	// execute manuallyCloseVoting()void
	callsub manuallyCloseVoting
	int 1
	return

// manuallyCloseVoting(): void
manuallyCloseVoting:
	proto 0 0

	// contracts/DaoWakanda.algo.ts:152
	// assert(this.txn.sender === this.app.creator, 'Only the creator is allowed to close the voting process.')
	txn Sender
	txna Applications 0
	app_params_get AppCreator
	pop
	==

	// Only the creator is allowed to close the voting process.
	assert

	// contracts/DaoWakanda.algo.ts:154
	// this.endDate.value = 0
	byte 0x656e6444617465 // "endDate"
	int 0
	app_global_put
	retsub

*create_NoOp:
	method "createApplication(string,string,uint64)void"
	txna ApplicationArgs 0
	match *abi_route_createApplication

	// this contract does not implement the given ABI method for create NoOp
	err

*call_NoOp:
	method "getDaoDetails()(string,string,uint64,uint64,uint64,uint64,uint64,uint64)"
	method "createDaoASA(pay)uint64"
	method "register(asset)void"
	method "vote(asset,bool)void"
	method "manuallyCloseVoting()void"
	txna ApplicationArgs 0
	match *abi_route_getDaoDetails *abi_route_createDaoASA *abi_route_register *abi_route_vote *abi_route_manuallyCloseVoting

	// this contract does not implement the given ABI method for call NoOp
	err

*process_static_tuple_element:
	proto 4 3
	frame_dig -4 // tuple head
	frame_dig -1 // element
	concat
	frame_dig -3 // tuple tail
	frame_dig -2 // head offset
	retsub

*process_dynamic_tuple_element:
	proto 4 3
	frame_dig -4 // tuple head
	frame_dig -2 // head offset
	concat
	frame_bury -4 // tuple head
	frame_dig -1 // element
	dup
	len
	frame_dig -2 // head offset
	btoi
	+
	itob
	extract 6 2
	frame_bury -2 // head offset
	frame_dig -3 // tuple tail
	swap
	concat
	frame_bury -3 // tuple tail
	frame_dig -4 // tuple head
	frame_dig -3 // tuple tail
	frame_dig -2 // head offset
	retsub